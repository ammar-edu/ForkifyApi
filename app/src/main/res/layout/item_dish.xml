<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/dish_name_textView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        app:layout_constraintEnd_toEndOf="@id/dish_image_view"
        app:layout_constraintStart_toStartOf="@id/dish_image_view"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Pizza long long long long long long long " />

    <ImageView
        android:id="@+id/dish_image_view"
        android:layout_width="150dp"
        android:layout_height="150dp"
        tools:background="@color/black"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/dish_name_textView" />

</androidx.constraintlayout.widget.ConstraintLayout>